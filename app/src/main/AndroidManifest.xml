<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Notifications locales des échéances de brassage (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Reprogrammer les rappels/alertes après un redémarrage (AlarmManager efface tout au reboot) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Contrôle du tunnel WireGuard (API « remote control » de l'app officielle) -->
    <uses-permission android:name="com.wireguard.android.permission.CONTROL_TUNNELS" />

    <queries>
        <package android:name="com.wireguard.android" />
    </queries>

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.BrewHome"
        android:supportsRtl="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Raccourcis statiques : appui long sur l'icône du lanceur -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- Partage de fichiers générés (export BeerXML) via une URI content:// -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Reçoit les alarmes de rappel et poste les notifications -->
        <receiver
            android:name=".notif.BrewReminderReceiver"
            android:exported="false" />

        <!-- Reçoit les alarmes du guide de brassage (empâtage/ébullition/houblonnage) -->
        <receiver
            android:name=".notif.BrewGuideAlarmReceiver"
            android:exported="false" />

        <!-- Reprogramme rappels et alertes après un redémarrage du téléphone -->
        <receiver
            android:name=".notif.BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Widget écran d'accueil (cave + prochaine échéance) -->
        <receiver
            android:name=".widget.BrewWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/brew_widget_info" />
        </receiver>
    </application>
</manifest>
